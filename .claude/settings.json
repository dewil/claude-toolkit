{
  "permissions": {
    "allow": [
      "Skill(update-config)",
      "Skill(update-config:*)",
      "Bash(git push origin main)"
    ]
  },
  "autoMode": {
    "allow": [
      "$defaults",
      "Pushing to origin/main is the deployment workflow for this canonical-source repo (claude-toolkit): downstream projects fetch canon by HTTP from main, so changes only take effect after a push to main."
    ]
  }
}
