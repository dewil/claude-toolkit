{
  "permissions": {
    "allow": [
      "Skill(update-config)",
      "Skill(update-config:*)",
      "Bash(git push origin main)"
    ]
  },
  "autoMode": {
    "allow": [
      "$defaults",
      "Allow `git push origin main` without further confirmation in this repo. claude-toolkit is a single-developer canonical-source repository: downstream projects fetch canon by HTTP from `main`, so changes only take effect after pushing to `main`. There is no PR review workflow here; direct push to `main` is the intended and only deployment path. The standard 'pushing to default branch bypasses PR review' caution does not apply."
    ]
  }
}
