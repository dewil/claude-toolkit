---
description: Структура vault'а - разделы, теги, локальные конвенции конкретного проекта
---

# Структура vault'а

TODO: одно-два предложения - о чем этот vault, какой домен.

Этот файл фиксирует конвенции **именно этого** vault'а. Где они расходятся с канон-дефолтами из `wiki-notes-style.md` (стиль заметки) и `wiki-linking-obsidian.md` (перелинковка) - **локальная конвенция здесь имеет приоритет**: единообразие vault'а важнее канон-дефолта. Все отклонения от дефолтов записывай сюда явно, чтобы агенты (`note-writer`, `librarian`) следовали локальному стилю, а не общему.

## Разделы (папки верхнего уровня)

- `inbox/` - входящие черновики, еще не разложенные по темам.
- TODO: перечисли тематические разделы-домены (например, `<Домен>/`), по одному на крупную тему. Если поверх лежит управленческая или иная обвязка (встречи, планы, решения) - перечисли и ее папки.

## MOC (map of content)

Роль карт раздела играют хаб-заметки - крупные навигационные заметки со списками `[[ссылок]]`. Новый хаб заводи, когда у темы накопилось 5+ заметок (см. `wiki-linking-obsidian.md`).

TODO: зафиксируй локальную конвенцию имени хаба и перечисли существующие MOC.

- Конвенция имени хаба: TODO (канон-дефолт - `<тема>-moc.md`; если в этом vault'е принят сорт-префикс, например `00 <Тема>.md`, - запиши его здесь).
- Существующие MOC: TODO (например, `<тема>-moc.md` - входная карта по теме).

## Теги

Иерархические через `/`. Один корневой тег на тематический домен:

- TODO: перечисли корневые теги и их подтеги (например, `<домен>/<подтема>`).

Сквозные теги (статусы, типы) - срез поверх папок, не копия дерева каталогов:

- `draft` - черновик, не готово.
- `archive` - устарело, оставлено для истории.
- TODO: добавь свои сквозные теги, если нужны.

Не дублируй структуру папок тегами (см. `wiki-linking-obsidian.md`): тег - сквозной срез, а не зеркало дерева папок. Если теги в этом vault'е используются мало или `frontmatter.tags` не обязателен (отклонение от `wiki-notes-style.md`) - зафиксируй это решение здесь.

## Конвенции имени файла

Канон-дефолт (`wiki-notes-style.md`) - kebab-case, без дат и числовых префиксов, имя по сущности. Если этот vault отклоняется от дефолта - **запиши локальный стиль здесь, он имеет приоритет**:

- Регистр и алфавит: TODO (латиница или кириллица; kebab-case / Title Case / пробелы).
- Сорт-префиксы: TODO (канон-дефолт - без них; если приняты числовые префиксы для упорядоченных разделов, например `01 `, - запиши).
- Имя - по сущности заметки, не по описанию ("Схема анализа X", не "Обзор схемы анализа X").

Связи между заметками - всегда `[[wiki-link]]` (правила - в `wiki-linking-obsidian.md`).
