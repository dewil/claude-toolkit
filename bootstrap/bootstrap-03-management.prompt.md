# Промт: scaffold для управленческих проектов

Этот файл - инструкция для Claude Code. Запускать **после** `bootstrap-02-scaffold.prompt.md`: открой Claude Code в **корне проекта** и попроси выполнить этот файл (например: `выполни инструкции из <путь>/bootstrap-03-management.prompt.md`).

Промт безопасен для существующих проектов. Сначала аудит, действия - после "ок". Существующие файлы не перезаписываются.

Для проектов, где AI ничего не забывает, ведет саммари встреч, генерит планы, контролирует команду.

Добавляет к общему scaffold:

- агента: `tracker` (контроль статусов, дедлайнов, владельцев)
- правила:
  - `meetings.md` - формат заметок встреч
  - `tasks-tracking.md` - формат задач и планов
  - `artifacts-structure.md` - структура папок управленческого проекта
  - `meeting-transcripts.md` - двойной формат транскрипций (KTalk + mymeet) и резюме `.summary.md`
  - `name-cross-check.md` - сверка имен из транскрипций по справочнику команды/стейкхолдеров
  - `google-sheets-mcp.md` - работа с Google Sheets через MCP `google-workspace`
  - `estimates-in-hours.md` - оценки в часах, не в человеко-днях
- папки: `Встречи/`, `Планы/`, `Решения/`, `Команда/`
- `@-`ссылки на новые правила в `CLAUDE.md`
- (опционально) шаблон CLAUDE.md - структура из 11 разделов под управленческий проект (журнал решений, глоссарий, действующие лица, расписание синков и т.д.). Это локальный шаблон с TODO-заглушками, не из канона.

---

КОНТЕКСТ ЦЕПОЧКИ. Это шаг 03. **Источник этого файла** (URL или локальный путь) определяет базу для канонических файлов:

- `canon_base` = подняться на один уровень из `bootstrap/`. Например, если этот файл загружен по URL `.../main/bootstrap/bootstrap-03-management.prompt.md`, то `canon_base = .../main/`.
- Канонические агенты и правила специализации: `<canon_base>/agents/*.md`, `<canon_base>/rules/*.md`.
- Предыдущий шаг (сиблинг): `bootstrap-02-scaffold.prompt.md` в той же папке.

Ты в корне управленческого проекта. Раскатываешь специализацию scaffold под управление: агент-трекер, правила формата встреч и задач, структура папок для артефактов. Базовый scaffold должен быть уже на месте (bootstrap-02-scaffold).

ВАЖНО. Промт безопасен для существующих проектов. Сначала аудит, потом план, потом ждешь "ок", только потом действуешь. Существующие файлы НЕ перезаписываешь.

ШАГ 1. Аудит (только чтение).

- pwd - корень проекта.
- Перечисли существующие:
  - .claude/agents/tracker.md
  - .claude/rules/meetings.md, tasks-tracking.md, artifacts-structure.md, meeting-transcripts.md, name-cross-check.md, google-sheets-mcp.md, estimates-in-hours.md
  - .claude/canon.yaml - есть ли (должен быть после bootstrap-02), что в `files`
  - CLAUDE.md - какие @-ссылки уже есть, и какие из 11 шаблонных разделов уже заполнены (О проекте, Действующие лица, Глоссарий, Структура каталогов, Ключевые файлы, Встречи, Предпочтения по стилю, Рабочие процессы, Открытые задачи, Решения и договоренности, Ссылки и ресурсы, Полезные команды)
  - Папки Встречи/, Планы/, Решения/, Команда/ - что есть, что нет
  - Если есть Встречи/ или Планы/ - посмотри top-3 файла в каждой, чтобы понять формат, и сообщи. Не правь.

ШАГ 2. Целевое состояние.

A. .claude/agents/ (источник - `<canon_base>/agents/`):
   - tracker.md

B. .claude/rules/ (источник - `<canon_base>/rules/`):
   - meetings.md
   - tasks-tracking.md
   - artifacts-structure.md
   - meeting-transcripts.md
   - name-cross-check.md
   - google-sheets-mcp.md
   - estimates-in-hours.md

C. CLAUDE.md содержит ссылки на все 7 правил выше в формате `@.claude/rules/<имя>.md`.

D. Папки в корне: Встречи/, Планы/, Решения/, Команда/.

E. `.claude/canon.yaml`:
   - `files` содержит 8 файлов специализации (7 rules + 1 agent).
   - `project_type` равен `management` (если пусто/отсутствует - проставляется на ШАГ 4).

F. (опционально, по подтверждению) CLAUDE.md заполнен по шаблону из 11 разделов (см. блок "ШАБЛОН CLAUDE.md" ниже). Если CLAUDE.md уже существует и заполнен - не перезаписываешь, предлагаешь сравнить и при необходимости дополнить недостающими разделами.

ШАГ 3. План.

Таблица: файл / папка / действие / статус. Для существующих - "не трону". Для отсутствующих - "создам".

Для CLAUDE.md - если файла нет или нет @-ссылок - покажи, что добавишь.

Для canon.yaml - покажи, какие 8 записей допишешь в `files`.

Если в Встречи/ или Планы/ обнаружен другой формат файлов - укажи в плане: "формат в проекте отличается от шаблона, что делаешь - пропускаешь правило или адаптируешь?". Жди ответа.

Жди "ок". Без подтверждения - не действуй.

ШАГ 4. Действуй (после "ок").

### 4a. Скопируй канонического агента и правила из `<canon_base>`

Для каждого файла из ШАГ 2 (A + B), которого еще нет в проекте:

- Если этот промт загружен по HTTP: `WebFetch <canon_base>/agents/<имя>.md` -> запиши в `.claude/agents/<имя>.md`. Аналогично для `<canon_base>/rules/<имя>.md` -> `.claude/rules/<имя>.md`.
- Локально (если читался с диска): `cp <canon_base>/agents/<имя>.md .claude/agents/<имя>.md`. Аналогично для rules.

Существующие файлы НЕ перезаписываются. Для апгрейда к актуальной версии канона есть `migrations/sync-from-canon.prompt.md`.

### 4b. Создай папки (если отсутствуют)

`Встречи/`, `Планы/`, `Решения/`, `Команда/` с `.gitkeep` в каждой.

### 4c. Допиши `.claude/canon.yaml`

В секцию `files` добавь записи, которых там еще нет:

```yaml
  - rules/meetings.md
  - rules/tasks-tracking.md
  - rules/artifacts-structure.md
  - rules/meeting-transcripts.md
  - rules/name-cross-check.md
  - rules/google-sheets-mcp.md
  - rules/estimates-in-hours.md
  - agents/tracker.md
```

Если `canon.yaml` нет - значит bootstrap-02 не выполнялся; сообщи об этом и не создавай `canon.yaml` сам (это работа шага 02).

Отдельно: если в `canon.yaml` поле `project_type` пустое (`""`) или отсутствует - проставь `project_type: management` на верхнем уровне. Если стоит другой тип - сообщи и спроси, оставить или переписать (это может быть миграция типа проекта).

### 4d. Дополни существующие файлы

Дополнения CLAUDE.md (ссылки на новые правила и опциональное заполнение по шаблону) - по отдельному "ок" на каждый пункт.

ШАГ 5. Отчет.

Таблица: файл / папка / действие. Что осталось вручную (например, перенос существующих заметок в стандартизированный формат, заполнение значений в шаблоне CLAUDE.md).

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ШАБЛОН CLAUDE.md (по подтверждению)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Этот шаблон применяй только если CLAUDE.md в корне проекта отсутствует ИЛИ существует, но почти пустой (только заглушки от bootstrap-02). Если CLAUDE.md уже содержательный - не перезаписывай. Предложи сравнить и точечно дополнить недостающими разделами.

Шаблон - обезличенный, локальный (не из канона - каждый проект подставляет свои значения). Подставь значения из проекта; если значения еще нет - оставь TODO с пометкой, что заполнить.

--- ФАЙЛ: CLAUDE.md ---
# CLAUDE.md

> Рабочая память проекта для Claude. Здесь храню ключевой контекст, чтобы быстро включаться в задачи без долгих расспросов. Обновляю файл по ходу работы.

- Автор: <TODO>
- Создан: <YYYY-MM-DD>
- Последнее обновление: <YYYY-MM-DD>

---

## Подключенные правила

@.claude/rules/typography-ru.md
@.claude/rules/subagents-usage.md
@.claude/rules/docs-maintenance.md
@.claude/rules/meetings.md
@.claude/rules/tasks-tracking.md
@.claude/rules/artifacts-structure.md
@.claude/rules/meeting-transcripts.md
@.claude/rules/name-cross-check.md
@.claude/rules/google-sheets-mcp.md
@.claude/rules/estimates-in-hours.md

---

## 1. О проекте

- Название: <TODO>
- Краткое описание: <одно-два предложения о цели проекта>
- Текущий этап: <инициация / разработка / приемка / поддержка>
- Ключевые сроки:
  - <YYYY-MM-DD> - <веха 1>
  - <YYYY-MM-DD> - <веха 2>

## 2. Действующие лица

| Данные | Ссылка |
| ------ | ------ |
| Команда проекта (ФИО, должности, контакты, отпуска) | <ссылка на таблицу> |
| Реестр стейкхолдеров (со стороны заказчика) | <ссылка> |
| План-график | <ссылка> |
| 1-to-1 встречи | <ссылка> |

## 3. Глоссарий и сокращения

> Список внутренних терминов, аббревиатур, кодовых названий и прозвищ. Расшифровываю их здесь, чтобы Claude понимал "с полуслова".

- <термин> - <расшифровка / контекст>

## 4. Структура каталогов

```text
.
├── CLAUDE.md          # этот файл
├── Встречи/           # заметки и транскрипции встреч (см. раздел 5а)
├── Планы/             # активные планы и задачи
├── Решения/           # важные решения отдельными файлами
├── Команда/           # профили команды и контакты
├── Docs/              # постоянная документация
├── scripts/           # рабочие скрипты
└── .claude/
    ├── rules/         # правила, подключенные через @-ссылки выше
    ├── agents/        # кастомные субагенты
    └── memory/        # автопамять проекта
```

## 5. Ключевые файлы

- <путь> - <назначение и правила обращения>

## 5а. Встречи

Расшифровки организованы по адресатам и периодам. Формат имени файла: `ГГГГ-ММ-ДД <короткая-тема>.<ext>`.

Источники транскрипций (см. `meeting-transcripts.md`):
- `.txt` - KTalk: точные таймкоды и спикеры.
- `.md` + `.json` - mymeet.ai: смысл, структура, программная обработка.
- `.summary.md` - верхнеуровневое резюме под отправку (не удаляем raw).

Структура:
- `Встречи/с заказчиком/<год>/<период>/`
- `Встречи/с командой/<год>/<месяц>/` - дейли
- `Встречи/с руководством/`
- `Встречи/1-1/<ФИО>/`
- `Встречи/Чаты/` - экспорты рабочих чатов (опционально)

Регулярное расписание (если применимо):
- Синк с заказчиком - <день недели> в <время>
- Дейли с командой - <день/каждый день> в <время>

## 6. Предпочтения по стилю

- Отвечаю на русском, от первого лица.
- Типографика - см. `@.claude/rules/typography-ru.md`.
- В новых документах Word/Excel автор - <TODO>.
- Перед отправкой результата всегда проверяю его (формулы, ссылки, орфографию).

## 7. Рабочие процессы

- Для таблиц - openpyxl + пересчет формул через скилл `xlsx`.
- Для документов - python-docx (через скилл `docx`).
- Для презентаций - скилл `pptx`.
- Для PDF - скилл `pdf`.
- Длинные задачи разбиваю через TodoList и завершаю шагом верификации.

## 7а. Работа с Google Sheets

См. `@.claude/rules/google-sheets-mcp.md`. Здесь - проектные значения:

- Аккаунт для MCP: <email>
- Основная таблица проекта: <spreadsheet_id> (`<ссылка>`)
- Имена листов:
  - `<имя>` (gid=<число>) - <назначение>

## 7б. Сверка имен из транскрипций

См. `@.claude/rules/name-cross-check.md`. Источники:
- Команда - <gid и имя листа>.
- Стейкхолдеры - <gid и имя листа>.

## 8. Открытые задачи

- <TODO>

## 8а. Задачи команды (если применимо)

- Папка `Задачи/` - выгрузка из <Redmine/Jira/...>, не наш реестр.
- Источник истины - <система>; снимок устаревает.
- Правки вручную перезатрутся следующей выгрузкой.

## 9. Решения и договоренности

> Краткий журнал значимых решений, чтобы потом не возвращаться к одному и тому же вопросу.

- <YYYY-MM-DD> - <решение и обоснование>.

## 10. Ссылки и ресурсы

- <название> - <ссылка>

## 11. Полезные команды

```bash
# пример: пересчет формул в Excel
# (заполни под рабочий скилл/утилиту проекта)
```

---

*Этот файл - живой документ. Если нахожу новый контекст или меняю подход, обновляю соответствующий раздел и фиксирую дату в шапке.*
--- КОНЕЦ ФАЙЛА ---

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ПРАВИЛА БЕЗОПАСНОСТИ
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- Без моего "ок" после плана - никаких изменений.
- Существующие файлы не перезаписываешь.
- Если формат Встречи/Планы уже сложился в проекте и отличается от шаблона - не навязывай шаблон, спроси.
- Дополнения CLAUDE.md - только по отдельному "ок".

---

После завершения:

- Заполни `Команда/people.md` ролями и контактами.
- Если у проекта свои секции в заметках встреч - дополни `.claude/rules/meetings.md` локально либо апстримь через upstream-candidate-механику sync.
- В `Планы/current.md` зафиксируй то, что в работе сейчас.
- Заполни значения в `7а. Работа с Google Sheets` (spreadsheet_id, имена листов с gid) и в `7б. Сверка имен` (gid таблиц команды/стейкхолдеров) - из реальных таблиц проекта.
- Для обновления канонических агентов/правил в будущем используй `migrations/sync-from-canon.prompt.md`.
